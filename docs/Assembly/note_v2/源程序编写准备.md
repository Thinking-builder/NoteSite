
# 实现第一个源程序

> 我们采用的是DOSbox编译环境

我们一般使用记事本会这Visual Studio等文本编辑器来编辑一个源程序，然后放入到MASM环境中运行，或者使用DOSBOX来进行Debug。在文本编辑器中，我们可以写一写**伪指令**，而在Debug状态下，我们的**伪指令**不起作用

## 编译指令

最好在编译之前把相关的``.asm``文件放入到相同的工作目录下。

```bash
> MASM
Source filename [.ASM]:
```

## 连接指令

```bash
> link
Object Moodules [.OBJ]:
```
